# MT5 Trend Bot - requirements.txt
# Core runtime dependencies (February 2026)

MetaTrader5>=5.0.45          # Official MetaQuotes Python package - stable releases from late 2024 onward
pandas>=2.2.0                # Data frames, time series handling (pandas 2.2+ is standard in 2025–2026)
numpy>=1.26.0                # Numerical computations (pairs well with recent pandas)
python-dotenv>=1.0.1         # Load credentials securely from .env files

pytz>=2024.1                 # Reliable timezone handling (very important for trading timestamps)
requests>=2.32.0             # Useful for economic calendar, news APIs, broker status checks, etc.

# ───────────────────────────────────────────────
# Optional but very useful for strategy development & visualization
scipy>=1.13.0                # Statistical tests, signal processing, curve fitting
matplotlib>=3.9.0            # Equity curve plotting, indicator visualization

# ───────────────────────────────────────────────
# Testing & quality tools (install separately or via extras)
# Recommended command: pip install -r requirements-dev.txt
pytest>=8.3.0
pytest-cov>=5.0.0
ruff>=0.6.0                  # Fast linting + formatting (replaces black/flake8/isort in many 2026 projects)
mypy>=1.11.0                 # Static type checking
pre-commit>=4.0.0            # Git hooks for consistent code style
